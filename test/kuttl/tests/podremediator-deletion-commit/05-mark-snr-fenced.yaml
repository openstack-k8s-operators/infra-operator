apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |
      oc patch selfnoderemediation podremediator-lease-node-snr -n "$NAMESPACE" --subresource=status --type=merge --patch '{"status":{"phase":"Fencing-Completed"}}'
